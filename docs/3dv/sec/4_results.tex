\section{Results}

\subsection{Real capture crops as the labeler input}
\label{sec:component}

The label stage classified each delivered instance from three isolated point-splat
renders. Replacing those images with real capture RGB crops --- and changing nothing else
--- produces the largest improvement measured on any oracle-free prediction path in this
study (\cref{tab:labels}).

\begin{table}[t]
  \centering\footnotesize
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{lcccc}
    \toprule
    scene & splat t-1 & rgb t-1 & splat t-3 & rgb t-3\\
    \midrule
    A & 0/7 \rid{C05} & 5/7 \rid{C07} & 0/7 \rid{C06} & 7/7 \rid{C08}\\
    B & 1/9 \rid{C09} & 5/9 \rid{C11} & 4/9 \rid{C10} & 8/9 \rid{C12}\\
    C & 0/5 \rid{C13} & 2/5 \rid{C15} & 0/5 \rid{C14} & 3/5 \rid{C16}\\
    \midrule
    \textbf{pooled} & \textbf{1/21} \rid{C01} & \textbf{12/21} \rid{C02}
      & \textbf{4/21} \rid{C03} & \textbf{18/21} \rid{C04}\\
    \bottomrule
  \end{tabular}
  \caption{Matched-instance label accuracy. Only the images changed: weights, vocabulary,
  $\text{top\_k}=3$, admission threshold, evaluator, delivered partitions and IoU
  matching are held fixed, and no constant was tuned between scenes. The denominator is
  oracle-selected; this is not end-to-end performance.}
  \label{tab:labels}
\end{table}

\paragraph{Paired, the effect is one-directional.}
Both arms were scored on the same matched instances, so the comparison is paired by
construction. At top-1, RGB fixes 12 instances and regresses 1 (exact McNemar
$p = 0.0034$ \rid{G01}; an instance-level statement only, since the 21 instances sit
within three scenes and the test treats them as independent, which they are not). At
top-3 it fixes 14 and regresses none ($p = 0.00012$ \rid{G02}, same caveat). A
scene-level sign test cannot reach significance with three clusters: all three scenes
improve, and $p = \SRSignTestP$ is the minimum attainable. The exact test construction
and the full clustering discussion are in the supplement.

\paragraph{A control constrains the interpretation.}
A wide crop of a doorway reads as a kitchen, so a naive RGB win could have been the model
recognising \emph{rooms} rather than \emph{objects}. A third arm with wider context and
the target dimmed, run on the development scene only (7 of the 21 pooled instances),
scores \textbf{below} the tight arm at both ranks --- 3/7 \rid{C17} and 5/7 \rid{C18}
against that scene's 5/7 \rid{C07} and 7/7 \rid{C08}. More context measurably hurts,
which \textbf{supports} the interpretation that the gain comes from object texture rather
than room gist. It does not prove it.

One observation is worth recording for what it suggests rather than what it shows: six of
the 21 matched instances kept the canonical class in the top three and lost it to hard
top-1 commitment \rid{G07}. We make no claim that retaining those hypotheses would
improve question answering --- that has not been tested.

\paragraph{Scope.}
This is \texttt{oracle\_free\_component\_eval}. The prediction path consumes no oracle
and no human key, but the denominator of 21 is instances the evaluator had already
matched to an annotation box. The number is conditional on detection having succeeded and
says nothing about detection itself: on the same scenes the delivered pipeline recovered
7/18 \rid{C29}, 9/20 \rid{C30} and 5/6 \rid{C31} annotated entities. \textbf{This result
must not be reported as end-to-end or deployable performance.}

\subsection{Held but unreachable}
\label{sec:unreachable}

The central experiment asks twelve questions about \texttt{NEAR} --- the one relation the
delivered graph actually contains --- across two rooms, with ten items scored after one
of the authors marked two ambiguous \rid{F65}. Four arms form a controlled identity
substitution over the same stored relations, and one external deployable comparator
answers the same questions from images alone (\cref{tab:arms}).

\begin{table}[t]
  \centering\footnotesize
  \setlength{\tabcolsep}{3pt}
  (a) controlled identity substitution\\[3pt]
  \begin{tabular}{@{}llcc l@{}}
    \toprule
    arm & identity & correct & cov. & scope\\
    \midrule
    geometry ceiling & human & 7/10 \rid{F28} & 0.80 & \texttt{id\_oracle}\\
    stored-edge replay & human & 7/10 \rid{F35} & 0.80 & \texttt{id\_oracle}\\
    delivered graph & labels & 0/10 \rid{F40} & 0.00 & \texttt{delivered}\\
    grounded graph & bridge & 2/10 \rid{F45} & 0.20 & \texttt{delivered}\\
    \bottomrule
  \end{tabular}\\[7pt]
  (b) external deployable comparator\\[3pt]
  \begin{tabular}{@{}llcc l@{}}
    \toprule
    arm & identity & correct & cov. & scope\\
    \midrule
    direct RGB & images & 7/10 \rid{F50} & 0.90 & \texttt{deployable}\\
    \bottomrule
  \end{tabular}
  \caption{(a) The controlled substitution: the geometry ceiling recomputes geometry,
  the other three arms read the same stored edges, and the source of object identity is
  the only varied input. The two 7/10 rows consume human-supplied identity and are
  bounds, not performance. (b) Direct multiview RGB is not a fifth substitution row: it
  follows a different path that bypasses the graph entirely, under one named model
  (claude-opus-5), one frozen pass, so its numbers enter only as labeled cross-path
  comparisons.}
  \label{tab:arms}
\end{table}

The direct-RGB row carries a cost the correct-count hides: on these development rooms it
was \textbf{wrong on 2 of 10} \rid{F51}. That is the baseline against which
\cref{sec:transfer}'s pilot-room behaviour must be read.

\paragraph{No additional serialization loss was measured.}
The stored-edge replay reads \emph{only} edges the extractor serialized and recomputes no
geometry. It agrees with the recomputed geometry ceiling on all 12 authored items
\rid{F63}: no additional loss was measured during serialization relative to recomputation
under the same convention. The scope is \texttt{identity\_oracle} and the limits are
real: \textbf{both compared arms consume human-supplied identity}, the comparison covers
the single \texttt{NEAR} relation, and because both sides apply the same 1.0\,m
surface-to-surface convention the check catches a serialization error but \textbf{cannot}
detect a wrong convention. Relation availability and serialization consistency were
observed on these items; semantic correctness of the stored relations was not
independently established.

\paragraph{Identity is what binds.}
Seven of the ten scored items are \emph{ceiling-correct, delivered-unanswered} \rid{F64}:
the delivered pipeline could not address the questions at all. Delivered-graph-unique
correct answers were 0 against a predeclared bar of 2 \rid{F60}.

\paragraph{An oracle-free bridge does not close it.}
A grounding bridge encoding anchor phrases and real capture crops with the same pinned
weights, admitting a match only on cross-view agreement and with no confidence threshold
anywhere, moved the delivered graph arm from 0/10 \rid{F40} to 2/10 \rid{F45} --- a
2-versus-0 difference that is not statistically separable at $n = 10$ --- and
failed all three predeclared gates: anchor precision 0.583 against $\ge 0.80$ \rid{F67},
coverage 0.467 against $\ge 0.60$ \rid{F68}, and graph-unique wins 0 against $\ge 2$
\rid{F69}. Of the 17 question anchors, 15 were human-resolvable, the bridge admitted 12,
and 7 of those admissions were correct \rid{F70}. One failure is diagnostic: the bridge
admitted a UID for an object one of the authors had marked absent from the delivered
partition,
with maximum cross-view agreement \rid{F75}. Cross-view agreement measures
\emph{consistency}, not \emph{existence}.

\paragraph{A ledger makes the transitions explicit.}
Rather than argue about where the capability goes, we record for each question whether it
survived each stage it must pass (\cref{fig:reach}). Of the 10 scored questions, 8 survive
object delivery \rid{G03} and all 8 survive both relation expressibility and the
serialized-edge stage --- \textbf{no additional loss was measured at the serialized-edge
stage relative to recomputation under the same convention} \rid{G05}.
Three survive anchor grounding \rid{G04}. The single largest loss, five of ten, is at
identity grounding, larger than every other transition combined. The substitution is
paired: 7 items are answered under human-supplied identity that the delivered graph does
not answer, none the other way (exact McNemar $p = 0.0156$ \rid{G06}, an instance-level
statement over ten items in two rooms; the test details are in the supplement).
\Cref{fig:qualitative} shows one such question in the room itself. Because one side
consumes human identity, it sizes a bound --- it says the arms differ, not that anything
deployable achieves 7/10.

Reachability is reported per arm, never as one mixed ladder: the delivered graph reaches
\SRArmDeliveredLadder{} across its declared stages, the grounded graph
\SRArmGroundedLadder{}, the stored-edge replay \SRArmStoredLadder{} (identity injected,
grounding bypassed), and direct RGB \SRArmDirectLadder{} (graph stages bypassed)
% NOTE for the integrator: the reserved code G07 for per-arm ARKit equivalence is
% already occupied in the registry by the top-1-commitment row (6/21), which this paper
% cites in Sec 4.1. G13 is used here so the collision fails loudly in the claim audit
% instead of resolving to the wrong row; add the registry row under G13 or renumber.
\rid{G13}. The evaluator re-derives each chain independently from the committed
artifacts, and the chains reproduce the committed statistics field for field.

\paragraph{Fault-localization validation.}
Attribution is only as trustworthy as its checkers, so we validated it under masking.
Into clean committed artifact chains --- evaluation key and entity artifact, relation
artifact, serialized graph, grounded candidates, answer --- we injected controlled
artifact-level faults, each mutating exactly one artifact, spanning \SRFaultClasses{}
fault classes and \SRFaultRelationTypes{} relation types (\texttt{NEAR},
\texttt{ON\_ENTITY\_SURFACE}, \texttt{ATTACHED\_TO}). The evaluator attributes via
independent per-stage checkers and was masked to the injected class --- it is never told
what was injected. It localized \SRFaultLocalized{} faults to the mutated stage, with
zero false attributions on clean artifacts \rid{G10}. This is evaluator-masked
artifact-level validation; it is not a blind study of the human protocol.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/fig4_reachability.pdf}
  \caption{Where questions are lost between representation and deployable answer. One row
  per stage an answer must survive. No additional loss was measured at the
  serialized-edge stage relative to recomputation under the same convention; identity
  grounding loses more than every other transition combined.}
  \label{fig:reach}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/fig5_qualitative.pdf}
  \caption{One question, in the room it is about. The stored \texttt{NEAR} edge between
  the basket and the desk is present, and its serialization is consistent with
  recomputation under the same convention (surface distance $0.0$\,m against a $1.0$\,m
  threshold); its semantic correctness was not independently established. A human
  resolving both referents answers it; the delivered labels
  never address it; the oracle-free bridge binds one referent and abstains on the other,
  so the edge is unreachable through it. Frames and crops are from the committed human
  review sheet; outcomes from the packed relation report.}
  \label{fig:qualitative}
\end{figure*}

\subsection{A blinded one-scene pilot under a twice-amended generator}
\label{sec:transfer}

Direct multiview RGB at 7/10 \rid{F50} was the strongest deployable arm, but both rooms
had been examined for months. For a pilot on a capture unused during development, the
gates, the scoring and the model-facing protocol --- view selection, prompt, schema and
two gates --- were frozen before any model run; question generation was amended twice on
question-text defects before scoring, with the chronology in the supplement. Question
generation reduces direct author selection: three agents independently enumerated objects from the same frames with no
access to each other or to any question; an object became an anchor on two-of-three
agreement; anchors were ordered mechanically and a fixed template allocated slots.

\paragraph{The generator was amended twice, and we state it plainly.}
Run 1's eight questions came back with four marked ambiguous, leaving $n = 4$ --- a size
at which the coverage gate degenerates into an anti-abstention gate. Run 1 was voided and
the generator amended twice, for scope wording and anchor ordering, then to restrict
relational slots to unique-referent anchors. What protects the result is narrower than
``we froze it first'': \textbf{the blinded model never ran in run 1}, so nothing scored
was selected on an outcome, and both amendments addressed defects visible in the
questions alone. Run 2 was authored and scored once at $n = 10$ \rid{F84}. A reader who
prefers the weaker reading --- that a generator was iterated twice on this scene before
producing a scorable set --- is entitled to it, and that is itself a finding about
fixed-procedure generation. The full amendment history is in the supplement.

\begin{table}[t]
  \centering\footnotesize
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{@{}lll@{}}
    \toprule
    metric & value & \\
    \midrule
    correct & 5/10 \rid{F76} & \\
    \textbf{wrong} & \textbf{0/10} \rid{F77} & never wrong\\
    unanswered & 5/10 \rid{F78} & \\
    acc.\ when answered & 1.000 \rid{F85} & over 5 answered\\
    \textbf{cross-view items} & \textbf{0/3} \rid{F79} & all declined\\
    exact acc.\ (gate $\ge 0.60$) & 0.50 \rid{F82} & \textbf{fail}\\
    coverage (gate $\ge 0.80$) & 0.50 \rid{F83} & \textbf{fail}\\
    \bottomrule
  \end{tabular}
  \caption{One pilot room, unused during development, scored once. Both predeclared
  gates failed, so no transfer claim is made \rid{F88}.}
  \label{tab:transfer}
\end{table}

The shape of the failure is the result. It is not an accuracy failure: every answer given
was correct \rid{F85} and none was wrong \rid{F77}. It is entirely coverage --- and
because exact accuracy counts an abstention as a non-correct item, a perfectly calibrated
abstainer fails both gates on one behaviour. The model declined precisely the category
that motivates the work.

We resist the phrase ``calibration transferred''. On the development rooms the same arm
was \textbf{wrong on 2 of 10} \rid{F51} at 0.90 coverage \rid{F53}, and its two-view
sufficiency gate never fired on any of the 7 thin-evidence items there \rid{F66}; on the
unseen room it declined 4 of 6 thin-evidence items and was right on both it answered
\rid{F87}. That is a \emph{change} in abstention behaviour between rooms, measured once on
each, not a property carried across. This also suggests a re-reading of the earlier 7/10:
those cross-view items were likely answerable from co-visible evidence.

\subsection{Evaluator transfer: Replica multi-relation QA}
\label{sec:replica}

To test that the \emph{evaluator} transfers --- not the pipeline --- we re-scored the
frozen Replica multi-relation QA scorecards (four scenes, \SRReplicaTotal{} items,
per-scene $n$ of \SRReplicaSceneNs{} \rid{G09}) through the same schema and outcome
mapping. Raw scorecard labels are preserved verbatim in every trace, and every metric is
keyed by (expected outcome, result) --- the normalized matrix and the raw categories are
reported side by side (\cref{tab:replica}).

\begin{table}[t]
  \centering\footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}lccc@{}}
    \toprule
    expected & correct & wrong & abstain\\
    \midrule
    answer & \SRReplicaAnswerCorrect{} & \SRReplicaAnswerWrong{} & \SRReplicaAnswerAbstain{}\\
    empty  & \SRReplicaEmptyCorrect{} & \SRReplicaEmptyWrong{} & ---\\
    \bottomrule
  \end{tabular}
  \hspace{1.2em}
  \begin{tabular}{@{}lc@{}}
    \toprule
    raw category & $n$\\
    \midrule
    true\_answer & \SRReplicaTrueAnswer{}\\
    true\_empty & \SRReplicaTrueEmpty{}\\
    miss & \SRReplicaMiss{}\\
    false\_answer & \SRReplicaFalseAnswer{}\\
    \bottomrule
  \end{tabular}
  \caption{Replica multi-relation QA re-scored through the evaluator \rid{G08}: the
  normalized (expected outcome, result) matrix, with the raw scorecard categories
  alongside. The \texttt{miss} category splits by the per-question record, never by the
  category label alone.}
  \label{tab:replica}
\end{table}

\paragraph{Aggregates mask.}
Pooled, the same scorecards would read \SRReplicaPooled{} ``correct'' --- a number
dominated by the \SRReplicaTrueEmpty{} true-empty items, on which being correct means
returning nothing. Keyed by expected outcome, the answered side is
\SRReplicaAnswerCorrect{} correct against \SRReplicaAnswerWrong{} wrong with
\SRReplicaAnswerAbstain{} abstentions \rid{G08}. The pooled number would mislead, and
the evaluator refuses to emit it.

\paragraph{Scope.}
The Replica arms consume oracle geometry and oracle identity. This demonstrates schema
and outcome transfer of the evaluator; it is not deployable performance, and it is not
full fault localization: every internal stage the packed scorecards cannot support is
reported \texttt{unknown} --- delivery, relation correctness and serialization are never
inferred from the final answer.
