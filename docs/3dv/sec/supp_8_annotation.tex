\section{Independent annotation}
\label{ssec:annotation}

The evaluation keys used by the main results were authored by a single
reviewer who is one of the authors and is not blind to the hypothesis, and
the paper says so wherever it matters. To measure how reproducible those
keys are, a second annotator --- blinded to all system outputs, to the arm
names, and to the hypothesis --- independently answered the 12 authored
relation questions from a self-contained review sheet presenting the same
frozen packet frames the blinded model received, in seeded-shuffled order:
the 10 items eligible for the primary evaluation, and the 2 excluded items,
retained deliberately so that the \emph{exclusion} judgement itself is
measured for reproducibility rather than assumed.

\begin{table}[t]
  \centering\footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}lccc@{}}
    \toprule
    field & $n$ & exact agreement & Cohen's $\kappa$\\
    \midrule
    answer (scored items) & \SRAnnScoredOf{} &
      \SRAnnScoredAgree{}/\SRAnnScoredOf{} & --- (free text)\\
    ambiguity flag & \SRAnnAmbiguityOf{} &
      \SRAnnAmbiguityAgree{}/\SRAnnAmbiguityOf{} & --- (degenerate)\\
    \bottomrule
  \end{tabular}
  \caption{Per-field agreement between the original key author and the
  blinded second annotator, after committed adjudication of free-text
  equivalences. $\kappa$ is reported only where a field's categories are
  finite and varied enough for chance correction to be meaningful; the
  second annotator marked no item ambiguous, so the ambiguity field is
  degenerate and only exact agreement is reported.}
  \label{tab:annotation}
\end{table}

The structure of the disagreements matters more than the count. Of the
\SRAnnScoredOf{} scored items, \SRAnnScoredAgree{} agree exactly. All
\SRAnnAbstainDisagreements{} pure answer disagreements are annotator
\emph{abstentions} (``cannot determine'', confidence 1 of 5): on no scored
item did the second annotator give a confidently wrong answer relative to
the key. The remaining disagreement is a set over-inclusion (the returned
near-set adds the anchor object itself alongside the key's single member).
On the two owner-excluded items, the outcomes split: one the annotator
answered confidently --- a genuine exclusion-judgement disagreement,
reported as such --- and one the annotator also could not determine,
functionally agreeing that the item is unanswerable. Ambiguity-flag
agreement is \SRAnnAmbiguityAgree{}/\SRAnnAmbiguityOf{}; the two
differences are exactly the owner-excluded items.

Referent descriptions and free-text relation labels are preserved verbatim
in the committed returned form for post-hoc analysis. The adjudication
overlay is itself a committed artifact and never overwrites the returned
form; every equivalence ruling carries a note. The returned review is
stored as evaluation-key reproducibility evidence and calibration data; no
key was altered in response to it for any result in this paper, and the
evaluated system was not retuned on it.
